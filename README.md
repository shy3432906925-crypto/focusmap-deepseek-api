# FocusMap DeepSeek API

这是 FocusMap v3 的 DeepSeek 后端代理项目。

部署到 Vercel 后，需要设置环境变量：

- DEEPSEEK_API_KEY：你的 DeepSeek API Key
- DEEPSEEK_MODEL：deepseek-v4-flash

部署后接口：

- /api/parse-tasks
- /api/day-review
